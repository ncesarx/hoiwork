# Sprint 002 — Design System e UI Foundation

## Commit

`feat(design-system): add tokens and foundational UI components`

## Entregas

- Pacote `@hoi/design-system` com tokens TypeScript e CSS.
- Pacote `@hoi/ui` com Button, Card, Container e Logo.
- Integração dos pacotes internos no website.
- Página de pré-visualização da identidade visual.
- Tema escuro inicial e estilos globais com Tailwind CSS 4.

## Validação

```bash
pnpm install
pnpm typecheck
pnpm --filter website dev
```

Acesse `http://localhost:3000`.
